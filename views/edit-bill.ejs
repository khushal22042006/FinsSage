<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Bill - FinSage</title>
  <link rel="icon" href="/images/logo.png.png" type="image/png">
  <link rel="stylesheet" href="/stylesheets/dashboard.css">
  <link rel="stylesheet" href="/stylesheets/dashboardmedia.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
</head>
<style>
  :root {
    --accent-color: #1F4E79;
    --text-color: #252424;
    --background-color: linear-gradient(90deg, #e2e2e2, #c9d6ff);
  }

  body {
    font-family: 'Inter', sans-serif;
    margin: 0;
    padding: 0;
    background: var(--background-color);
    color: var(--text-color);
    background-attachment: fixed;
    background-size: cover;
  }

  .dark-mode {
    --background-color: linear-gradient(90deg, #313b4d, #1e293b);
    --text-color: #e2e8f0;
    background: var(--background-color) !important;
    color: var(--text-color) !important;
  }

  h2.section-title {
    min-height: 3rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .fade-in {
    animation: fadeIn 0.5s ease-in-out forwards;
    opacity: 0;
  }

  @keyframes fadeIn {
    to { opacity: 1; }
  }

  .dark-mode .container {
    background-color: #38485f !important;
    color: var(--text-color);
    box-shadow: 0 0 10px rgba(0,0,0,0.3);
  }

  .dark-mode input,
  .dark-mode select {
    background-color: #1e293b;
    color: var(--text-color);
    border: 1px solid #475569;
  }

  .dark-mode input::placeholder {
    color: #94a3b8;
  }

  .dark-mode input:focus,
  .dark-mode select:focus {
    background-color: #1e293b;
    color: #fff;
    border-color: var(--accent-color);
    outline: none;
    box-shadow: 0 0 0 0.2rem rgba(31, 78, 121, 0.25);
  }

  .dark-mode label {
    color: var(--text-color);
  }

  .dark-mode .alert-success {
    background-color: #2e8b57;
    color: #fff;
  }

  .dark-mode .alert-danger {
    background-color: #c53030;
    color: #fff;
  }

  .dark-mode h2.section-title {
    background-color: var(--accent-color) !important;
    color: #fff !important;
  }

  .dark-mode .btn-primary {
    background: linear-gradient(to right, #2563eb, #1d4ed8) !important;
    border: none;
    color: #fff;
  }

  .dark-mode .btn-primary:hover {
    background: linear-gradient(to right, #1e40af, #1e3a8a) !important;
  }

  .dark-mode a.bg-gray-700 {
    background-color: #334155 !important;
    color: #e2e8f0;
  }

  .dark-mode a.bg-gray-700:hover {
    background-color: #1e293b !important;
  }
  .form-label{
  font-size:large;
  font-weight: 600;
}

</style>
<body id="body" class="min-h-screen">
  <div class="container my-5 max-w-xl mx-auto p-4 bg-white rounded-2xl shadow-md">
    <div class="text-center mb-4">
      <h2 class="text-2xl font-semibold text-[#FFFFFF] bg-[#1F4E79] section-title rounded-t-2xl">Edit Bill </h2>
    </div>


<form action="/<%= userId %>/bill/<%= bill._id %>/edit?_method=PUT" method="POST" class="space-y-4">
  <div>
    <label for="billName" class="form-label">Bill Name</label>
    <input type="text" name="billName" id="billName" class="form-control" placeholder="Enter bill name" required value="<%= bill.billName %>">
  </div>

  <div>
    <label for="amount" class="form-label">Amount</label>
    <input type="number" name="amount" id="amount" class="form-control" placeholder="Enter amount" required value="<%= bill.amount %>">
  </div>

  <div>
    <label for="dueDate" class="form-label">Due Date</label>
    <input type="date" name="dueDate" id="dueDate" class="form-control" required value="<%= bill.dueDate.toISOString().slice(0, 10) %>">
  </div>

  <div>
    <label for="repeat" class="form-label">Repeat</label>
    <select name="repeat" id="repeat" class="form-select">
      <option value="None" <%= bill.repeat === "None" ? "selected" : "" %>>None</option>
      <option value="Monthly" <%= bill.repeat === "Monthly" ? "selected" : "" %>>Monthly</option>
      <option value="Yearly" <%= bill.repeat === "Yearly" ? "selected" : "" %>>Yearly</option>
      <option value="Weekly" <%= bill.repeat === "Weekly" ? "selected" : "" %>>Weekly</option>
    </select>
  </div>

  <div class="form-check">
    <input type="checkbox" name="isPaid" value="true" class="form-check-input" id="isPaid" <%= bill.isPaid ? "checked" : "" %>>
    <label for="isPaid" class="form-check-label">Mark as paid</label>
  </div>

  <div class="text-center">
    <button type="submit" class="btn btn-primary px-4 py-2 rounded-lg bg-gradient-to-r from-blue-600 to-blue-500 text-white hover:from-blue-700 hover:to-blue-600 shadow-sm">Update Bill</button>
  </div>
</form>

    <div class="mt-4 text-center">
      <a href="/<%= userId %>/dashboard" class="inline-block px-4 py-2 text-white bg-gray-700 rounded-lg hover:bg-gray-800 text-sm">Back to Dashboard</a>
    </div>
  </div>

  <script>
    const theme = localStorage.getItem('theme');
    if (theme === 'dark') {
      document.body.classList.add('dark-mode');
    }
  </script>
</body>
</html>
